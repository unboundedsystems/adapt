{
  "name": "@usys/utils",
  "version": "0.0.1",
  "description": "Unbounded Adapt Utilities Library",
  "main": "dist/src/index.js",
  "typings": "dist/src/index.d.ts",
  "author": "Unbounded Systems",
  "bugs": {
    "url": "https://gitlab.com/unboundedsystems/adapt/issues"
  },
  "dependencies": {
    "decamelize": "^2.0.0",
    "execa": "^0.10.0",
    "fs-extra": "^6.0.1"
  },
  "devDependencies": {
    "@types/decamelize": "^1.2.0",
    "@types/execa": "^0.9.0",
    "@types/fs-extra": "^5.0.3",
    "@types/node": "^8.10.20",
    "mocha": "^5.2.0",
    "nyc": "^12.0.2",
    "should": "^13.2.1",
    "typescript": "^2.9.2",
    "verdaccio": "^3.2.0"
  },
  "peerDependencies": {},
  "files": [
    "/dist/src"
  ],
  "homepage": "https://gitlab.com/unboundedsystems/adapt",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@gitlab.com/unboundedsystems/adapt.git"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -w -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "tslint --project .",
    "lint-and-fix": "tslint --project . --fix",
    "pretest": "make build",
    "test": "nyc mocha --opts mocha.opts"
  }
}
