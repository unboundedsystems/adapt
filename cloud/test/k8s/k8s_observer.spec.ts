//import { gql } from "@usys/adapt";
import {} from "graphql";
import * as should from "should";

describe("k8s observer tests", () => {
    it("Schema transform experiments", function () {
        this.timeout(40000);

        should(undefined).Undefined();
    });
});
